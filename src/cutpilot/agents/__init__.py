"""Agent layer — the NAT workflow is declared in `configs/cutpilot.yml`.

The only Python here is the Scout function; Editor and Orchestrator are YAML-only
(NAT's built-in `tool_calling_agent` and `sequential_executor`)."""
